server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Routage côté client (Blazor WASM) : repli sur index.html.
    location / {
        try_files $uri $uri/ /index.html =404;
    }
}
